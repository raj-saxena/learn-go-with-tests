## Tasks

1. Tests for HTTP GET and HTTP POST (with body) calls -- use https://jsonplaceholder.typicode.com/ as the REST API
2. Test a client like https://github.com/google/go-github. Try writing a test for `func createTeam(name) { gh.createTeam() }`
3. GCP code to create a secret -- https://docs.cloud.google.com/secret-manager/docs/create-secret-quickstart